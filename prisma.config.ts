// prisma.config.ts is used by Prisma 7+.
// This project uses Prisma 5 (MongoDB requires the native connector,
// which is not yet supported in Prisma 7's new driver-adapter model).
// Database URL is configured via MONGO_URL in prisma/schema.prisma.
export {}
